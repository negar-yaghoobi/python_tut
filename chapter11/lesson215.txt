# pip freeze > requirements.txt ......> save all packages of project in requirements.txt file

# pip install -r requirements.txt   .....> install all packages in requirements.txt file in new environment of project